# Starry Village — 작업 문서 (Claude Code 진입점)

여아 타깃 HTML5 꾸미기 게임 + 짧은 연속 스토리 + 쇼츠/블로그로 **하나의 캐릭터
IP를 저비용으로 반복 노출**하는 프로젝트. 게임 회사 프로젝트가 아니라 **IP 공장**이다.
이 문서는 **왜 이렇게 만들었는지**를 담는다. 작업이 끝날 때마다 갱신한다.

최종 갱신: 2026-09-15 (프로젝트 생성)

읽는 순서: 이 파일 → `docs/00_MASTER.md` → 작업 대상 문서.
새 세션에서는 `HANDOFF.md` 의 "현재 상태"부터 본다.

---

## 1. 원칙 (불변)

편의를 위해 완화하지 않는다. 어긴 적이 있으면 `DECISIONS.md` 에 남긴다.

### 1-1. 게임은 단순하고, 이야기는 짧다
옷을 고르고 → 꾸미고 → 다음 이야기를 본다. 그게 전부다. 한 스테이지 20~40초,
대사 한 줄 40자 이내, 스테이지당 대사 6줄 이내. 텍스트가 길어지면 잘못 만든 것이다.

### 1-2. 큰 스토리는 고정, 표현은 무한
플레이어의 옷 선택은 **대사·장면만** 바꾼다. 보상·호감도·다음 스테이지는 동일하다.
분기 개발을 만들지 않는다. (`docs/00_MASTER.md` §4 "스토리 슬롯")

### 1-3. 코드는 고정, 데이터만 갈아끼운다
캐릭터·의상·스테이지·대사·보상은 전부 `data/*.json`. 코드에 숫자·이름·경로를
박지 않는다. 이미지는 `id → 파일` 매핑이라 **교체해도 코드는 안 건드린다.**

> 이유: 연재 중에는 AI 이미지를 그대로 쓰고, 반응이 확인된 캐릭터만 나중에
> 사람이 재설계해 **일괄 교체**한다 (§3). 교체 가능 구조가 없으면 그때 두 번 만든다.

### 1-4. 모든 에피소드는 콘텐츠 원료다
스테이지 1개 = 일러스트 + 쇼츠 2~3개 + 블로그 1개 + 히든 에피소드 후보.
한 번 쓰고 버리는 자산은 만들지 않는다. (`docs/03_CONTENT_PIPELINE.md`)

### 1-5. 먼저 만들고, 반응을 보고, 살아남은 것을 IP로 만든다
초기 품질 완벽주의 금지. 캐릭터 일관성은 "좋은 리아 한 장을 계속 참조"로 유지하고,
LoRA·인간 리디자인은 반응이 확인된 뒤에 한다.

### 1-6. 단일 HTML, 외부 의존 최소
`games/*/index.html` 하나에 HTML+CSS+JS. 프레임워크·번들러·서버·로그인·DB 없음.
360×640 세로 기준, 모바일 브라우저에서 파일 하나로 열린다. localStorage 저장.
AI가 2주 뒤에 다시 읽어도 구조를 이해하고 한 기능만 고칠 수 있어야 한다.

### 1-7. 원본 자산 무단 사용 금지
참고 게임(Anime Dress Up, Pastel Girl, Lily Diary 등)의 **루프·구조**는 참고하되
이미지·이름·사운드·UI 그래픽은 100% 자체 생성. 실존 IP 캐릭터 사용 금지.

### 1-8. 아동 타깃 광고 정책을 속이지 않는다
드레스업은 FTC/구글이 "아동 지향" 판단 요소로 본다. "여아용으로 만들고 전연령
표시" 식 우회 금지. 1차 KPI는 광고수익이 아니라 **노출당 제작비**와 **다음 스테이지
클릭률**이다.

---

## 2. 폴더 구조

```
starry-village/
├── CLAUDE.md            ← 이 파일. 원칙 + 왜
├── HANDOFF.md           ← 현재 상태 / 다음 할 일 (세션 시작 시 먼저)
├── README.md            ← 파일 역할표, 실행 방법
├── DECISIONS.md         ← D-번호 결정 기록 (되돌릴 때 이유를 본다)
├── DEVLOG.md            ← 날짜별 작업 일지 (짧게)
├── bugs_log.csv         ← 발견일,심각도,모듈,내용,조용히틀림,수정일
├── docs/
│   ├── 00_MASTER.md     ← 종합 구조: IP 전략·세계관·라인업·단계·툴
│   ├── 01_WORLD_BIBLE.md← 세계관·캐릭터 DNA·아트 규칙(3단)·이미지 프롬프트
│   ├── 02_GAME01_SPEC.md← 1호 「Dress Story」 화면·루프·데이터·완료조건
│   ├── 03_CONTENT_PIPELINE.md ← Story Seed → 게임/일러/쇼츠/블로그
│   └── 04_ANTIGRAVITY_HANDOFF.md ← 다른 툴로 넘길 때 절차
├── data/                ← 게임이 읽는 유일한 진실
│   ├── characters.json  ├── costumes.json  ├── stages/stage_001.json …
├── games/dress-story/index.html   ← 1호 게임 (단일 파일)
├── assets/characters|costumes|backgrounds|ui   ← id 로 참조되는 이미지
├── content/seeds|shorts|blog|images            ← 스테이지에서 파생된 콘텐츠
├── notes/               ← 리서치·회의 메모 (날짜_제목.md)
└── tools/               ← 검증 스크립트 (스테이지 JSON 검사 등)
```

---

## 3. 제작 단계 (현재 위치: ★)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| ★ P0 구조 | 문서·폴더·데이터 스키마 | Claude Code 가 이 폴더만 읽고 1호를 만들 수 있다 |
| P1 프로토 | 1호 Stage 1~3, 플레이스홀더 아트 | 폰 브라우저에서 10분 완주, NEXT 가 눌린다 |
| P2 아트 | 리아 마스터 이미지 + 의상 5벌 + 왕자 첫 등장 | 3장면에서 같은 캐릭터로 보인다 |
| P3 연재 | Stage 4~12 + 쇼츠·블로그 파생 | 주 2~3 스테이지, 스테이지당 쇼츠 2개 |
| P4 검증 | 4주 반응 확인 | 다음스테이지 클릭률·재방문·쇼츠 반응으로 캐릭터 생존 판정 |
| P5 IP화 | 인간 리디자인 → 이미지 일괄 교체 | 코드 수정 0, 데이터 매핑만 변경 |
| P6 확장 | 2호(카페)·3호(정원)… 같은 세계관 | 캐릭터 공유, 나중에 하나의 큰 게임으로 |

---

## 4. 작업 방식

### 4-1. 역할
- **Claude Code (이 PC)**: 기획 정제 + 코딩 + 실행 확인. 현재 단일 담당.
- **Claude (Cowork/채팅)**: 전략·세계관·리서치·문서 검토. 이 폴더의 문서를 같이 본다.
- **Antigravity (선택)**: 반복 코딩량이 커지면 넘긴다. `docs/04_ANTIGRAVITY_HANDOFF.md`.
- **사람(방가방가)**: 컨셉 판단, 캐릭터 최종 선택, 반응 판정, P5 리디자인.

### 4-2. PC 에서 개발, 폰에서 이어보기
```
cd C:\projects\starry-village
claude --remote-control
```
폰에서 같은 세션을 본다. 코드·실행은 이 PC에 남는다. 게임 확인은 폰 브라우저로
`index.html` 을 여는 게 가장 정확하다 (PC 에서 `python -m http.server 8080` 후 같은
와이파이에서 접속, 또는 파일을 폰으로 복사).

### 4-3. 작업 루프
```
문서/데이터 수정 → index.html 수정 → 브라우저 실행 → 스크린샷/플레이 → 차이 기록 → 수정
```
- 한 번에 전체를 다시 만들지 않는다. "Stage 2 대사만", "버튼 크기만" 처럼 좁게 고친다.
- 작업 완료 보고는 5줄 이내. 무엇이 바뀌었고, 무엇을 확인해야 하는지만.
- 세션 끝에 `HANDOFF.md` 현재 상태와 `DEVLOG.md` 한 줄을 갱신한다.

### 4-4. 하지 않는 것
React/Vue/번들러 도입, 서버·DB·로그인, 사용자 간 채팅, 스토리 분기 트리,
30스테이지 한 번에 생성, 리아 확정 전 5인 전원 정밀 제작, LoRA 학습(P5 이후).

---

## 5. 현재 상태 (2026-09-15)
- 세계관·캐릭터 4+1인·게임 구조·아트 3단 체계는 ChatGPT 세션에서 논의 완료 → `docs/` 로 이관.
- 리아 AI 컨셉 이미지 1세트 존재(ChatGPT 생성, 이 폴더에는 아직 없음). "가장 평범한 컨셉"으로 확정.
- 코드 0줄. 다음 작업은 `docs/02_GAME01_SPEC.md` 대로 `games/dress-story/index.html` 첫 버전.
- 상세는 `HANDOFF.md`.
